--- src/components/layout/ContextSidebar.tsx
+++ src/components/layout/ContextSidebar.tsx
@@ -4,6 +4,8 @@
 export function ContextSidebar({ currentView }: { currentView: string }) {
   const setActiveSession = useAppStore(state => state.setActiveSession)
   const setActiveWorkflow = useAppStore(state => state.setActiveWorkflow)
+  const sessions = useAppStore(state => state.sessions)
+  const workflows = useAppStore(state => state.workflows)
 
   const activeSessionId = useAppStore(state => state.activeSessionId)
   const activeWorkflowId = useAppStore(state => state.activeWorkflowId)
@@ -14,12 +16,11 @@
       case 'workflow':
         return {
           title: 'Workflows',
-          items: [
-            { id: 'w1', title: 'Data Processing Pipeline', active: activeWorkflowId === 'w1' },
-            { id: 'w2', title: 'Weekly Report Generator', active: activeWorkflowId === 'w2' },
-            { id: 'w3', title: 'User Onboarding Flow', active: activeWorkflowId === 'w3' },
-            { id: 'default-workflow', title: 'My First Workflow', active: activeWorkflowId === 'default-workflow' },
-          ]
+          items: workflows.map(w => ({
+            id: w.id,
+            title: w.name,
+            active: activeWorkflowId === w.id
+          }))
         };
       case 'agents':
         return {
@@ -51,11 +52,11 @@
       default:
         return {
           title: 'Chats',
-          items: [
-            { id: 'default-session', title: 'Project Planning', active: activeSessionId === 'default-session' },
-            { id: '2', title: 'UI Component Design', active: activeSessionId === '2' },
-            { id: '3', title: 'General Inquiry', active: activeSessionId === '3' },
-          ]
+          items: sessions.map(s => ({
+            id: s.id,
+            title: s.title,
+            active: activeSessionId === s.id
+          }))
         };
     }
   };
